import Typography from '../../components/Typography/Typography';

const NotFound = () => {
  return (
    <Typography variant="display-1" textAlign="center">
      Oops! We can't seem to find the page you're looking for.
    </Typography>
  );
};

export default NotFound;
